#pragma once
enum class GameState {
    START_SCREEN,
    TUTORIAL,
    KEYBOARD_SHORTCUTS,
    SELECT_MODE,
    SELECT_NUMBER_OF_PLAYERS,
    PLAYING,
    PAUSED,
    WINNING_TIME,
    // Other states...

};